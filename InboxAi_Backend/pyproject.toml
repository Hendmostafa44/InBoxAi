[project]
name = "inboxai-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "google-adk>=2.7.0",
    "litellm",
    "redis>=8.1.0",
    "uvicorn>=0.52.3",
]

[tool.fastapi]
entrypoint = "server:app"